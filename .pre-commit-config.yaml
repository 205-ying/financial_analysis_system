# Pre-commit hooks 配置
# 在提交前自动运行代码检查和格式化
# 
# 安装方式：
#   cd services/api
#   pip install pre-commit
#   pre-commit install
#
# 手动运行：
#   pre-commit run --all-files

repos:
  # Ruff - 快速Python linter和formatter
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.1.9
    hooks:
      # Ruff linter
      - id: ruff
        args: [ --fix ]
        files: ^services/api/
      # Ruff formatter
      - id: ruff-format
        files: ^services/api/

  # 通用检查
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
        exclude: ^apps/web/
      - id: end-of-file-fixer
        exclude: ^apps/web/
      - id: check-yaml
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-merge-conflict
      - id: check-json
        exclude: ^apps/web/(tsconfig|package)\.json$

  # Python特定检查
  - repo: https://github.com/PyCQA/isort
    rev: 5.12.0
    hooks:
      - id: isort
        files: ^services/api/
        args: ["--profile", "black"]

  # 前端 - ESLint
  - repo: https://github.com/pre-commit/mirrors-eslint
    rev: v8.54.0
    hooks:
      - id: eslint
        files: ^apps/web/src/.*\.[jt]sx?$
        types: [file]
        args: ['--fix']
        additional_dependencies:
          - eslint@8.54.0
          - eslint-plugin-vue@9.18.1
          - '@typescript-eslint/parser@6.12.0'
          - '@typescript-eslint/eslint-plugin@6.12.0'
        exclude: ^apps/web/(node_modules|dist)/

  # 前端 - Prettier
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v3.1.0
    hooks:
      - id: prettier
        files: ^apps/web/src/
        types_or: [javascript, jsx, ts, tsx, vue, css, scss, json]
        exclude: ^apps/web/(node_modules|dist)/
