# ===========================================
# 餐饮企业财务分析系统 - 环境配置模板
# ===========================================

# 应用基础配置
APP_NAME="餐饮企业财务分析系统"
APP_VERSION="1.0.0"
DEBUG=true
ENVIRONMENT=development

# 服务器配置
HOST=0.0.0.0
PORT=8000

# 数据库配置
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/financial_analysis
DATABASE_ECHO=false

# JWT 认证配置
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-2024
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30
JWT_REFRESH_EXPIRE_DAYS=7

# CORS 配置
CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173","http://localhost:3000"]
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=["*"]
CORS_ALLOW_HEADERS=["*"]

# API 配置
API_V1_PREFIX=/api/v1

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=logs/app.log
LOG_ROTATION=1 day
LOG_RETENTION=30 days

# Redis 配置（可选）
# REDIS_URL=redis://localhost:6379/0

# 分页配置
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# ===========================================
# 生产环境请修改以下配置：
# 1. 更改 JWT_SECRET_KEY 为随机生成的强密钥
# 2. 设置正确的数据库连接字符串
# 3. 配置适当的 CORS 域名
# 4. 设置 DEBUG=false
# 5. 配置日志文件路径
# ==========================================